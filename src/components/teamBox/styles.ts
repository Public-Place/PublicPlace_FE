import styled from "styled-components";

export const TeamBoxContainer = styled.div`
  width: calc(15rem - 1.4rem);
  height: calc(14rem - 1.4rem);

  margin-bottom: 1rem;

  padding: 0.7rem 0.7rem;

  border-radius: 15px;

  color: black;

  background-color: #ebebeb;
  box-shadow: 0 0 5px 1px rgb(0, 0, 0);
`;
